# tests/__init__.py
# -----------------
# Marks the tests directory as a Python package, enabling pytest to resolve
# sibling imports (e.g. ``from parser import parse_weather``) correctly when
# invoked from the project root.
